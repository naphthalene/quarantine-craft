import React from "react";
import { Typography } from "@material-ui/core";

/**
 * Home page of the server
 */
const Home: React.FC = () => {
  return (
    <div>
      <Typography variant="h2">Welcome to the server.</Typography>
      <Typography variant="body2">Hello</Typography>
    </div>
  );
};

export default Home;

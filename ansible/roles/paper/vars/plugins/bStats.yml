# bStats collects some data for plugin authors like how many servers are
# using their plugins.  To honor their work, you should not disable it.
# This has nearly no effect on the server performance!  Check out
# https://bStats.org/ to learn more :)
bStats:
  config:
    enabled: false
    serverUuid: 237aba13-e65d-4534-9d6e-9bc1a8c0f17a
    logFailedRequests: false

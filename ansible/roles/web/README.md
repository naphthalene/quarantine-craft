## Web server

The webserver hosts the following routes:

* /                     -> Single page app
* /atlas/               -> LiveAtlas app
* /tiles/               -> Rendered tiles
* /api/                 -> Server data directory
